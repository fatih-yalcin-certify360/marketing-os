{
  "$comment": "Development tooling that ships with the repo but not in any image. A real project so it is typechecked and linted like everything else.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
